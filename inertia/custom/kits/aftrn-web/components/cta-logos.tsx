/**
 * The two inline-SVG logo strips used in the CTA sections ("Built with" +
 * "Connects with your CRM & tools"). Defined once on the landing page and
 * re-exported here so every page's CTA shares the same markup without
 * duplicating ~15 large SVG paths.
 */
export { BuiltWithLogos, ConnectsLogos } from '../pages/home'
