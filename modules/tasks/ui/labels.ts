import type { ModulePageLabels } from '~/lib/module-labels'

/** See `inertia/lib/module-labels.ts` — core no longer names this module. */
export default {
  exact: { '/admin/tasks': 'To do list' },
} satisfies ModulePageLabels
