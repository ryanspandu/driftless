{
  "name": "driftless-mcp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP stdio server that builds Driftless sites via the builder-API.",
  "bin": {
    "driftless-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
